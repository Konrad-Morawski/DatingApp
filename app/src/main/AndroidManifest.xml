<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.kmorawski.sampleapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- not obligatory, but helps Glide dealing with network errors -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">

        <!-- starting screen, consisting of a profiles list -->
        <activity android:name=".views.ProfilesListActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- child screen, to which we navigate by selecting a profile we like -->
        <activity android:name=".views.ProfileDetailsActivity" />

    </application>

</manifest>